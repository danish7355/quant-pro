services:
  - type: web
    name: cryptofutur-bot
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 22.14.0
      - key: PORT
        value: "3000"
      - key: FIREBASE_CONFIG
        sync: false
